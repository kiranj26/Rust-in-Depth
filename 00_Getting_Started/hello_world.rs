// This is the main function, the entry point of the program
fn main() {
    // This line prints "Hello, World!" to the console
    // Using a micro means that you are calling a micro in rust
    // This would have been println if we wanted to call a function
    println!("Hello, World!");
}
